import type { ProductCategory, ProductStatus } from "../types";

export const CATEGORIES: ProductCategory[] = [
  "Electronics",
  "Clothing",
  "Food",
  "Books",
  "Other",
];

export const STATUSES: ProductStatus[] = ["active", "inactive", "archived"];
